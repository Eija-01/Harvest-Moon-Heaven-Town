// 1. CONFIG
// Fungsi: Berisi variabel pengaturan dasar yang bersifat tetap (konstanta).
// Fokus ke file ini jika ingin mengubah metrik engine dasar seperti kecepatan jalan karakter, ukuran base karakter, kecepatan lari, tinggi maksimal lompatan, atau skala dunia.

const jumpScale = 0.8; 
const baseCharSize = 130; 
const walkSpeed = 2; 
const runSpeed = 4; 
const FIXED_ZOOM = 1;
const jumpDuration = 600; 
const jumpMaxHeight = 150;
const angle = 25;
